#include <iostream>


/*
handleFSError method processes a simple errorCode returned by the filesystem based method, it directly outputs the meaning of an error on the standard output. 
TODO
the handleFSError outputs a meaning of an error on the error output
TODO
the handleFSError outputs a meaning of an error and a datetime of the error occurence in the log file (only when, and if logging is implemented)
*/
void handleFSError(int errorCode);
